export enum PostHogEvents {
  CHALLENGE_SEARCH_PERFORMED = "challenge_search_performed",
}

export enum PostHogProperties {
  SEARCH_QUERY = "search_query",
  SEARCH_COMPONENT = "search_component",
  RESULTS_COUNT = "results_count",
}
