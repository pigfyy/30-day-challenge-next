---
description: 
globs: 
alwaysApply: true
---
Never use space-x or space-y tailwind classes. Always prefer gap instead.

Always use shadcn components if possible, and always install reusable UI components from shadcn CLI if possible.


Always use absolute (@/) imports, instead of relative imports.